# Palindrome-Checker-App
// This is the third use case
// Uses string reverse
// Temp variable used, then compared
// Prints according to string
// Hardcoded
//This is the second use case for the palindrome app
// It uses hard codong logic, where we check if two words are palindromes or not
// The logic is as follows:
// We use the two-pointer method
// Create function isPalindrome
// Two variables left and right - for first and last letter
// For loop runs for length of string/2 iterations
// If left != right, return false - not a palindrome
// Else, return true - is a palindrome
// In main method, use two hard-coded strings to check
// Use print statement to print
